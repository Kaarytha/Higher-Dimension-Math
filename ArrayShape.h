#pragma once

#include "defines.h"

#include <vector>
#include <initializer_list>

namespace NAMESPACE {
	
	// ArrayShape currently is just a std::vector
	// but might need to be extended later
	typedef std::vector<std::size_t> ArrayShape;

}